{
  "name": "ssh-proxy-server",
  "version": "1.0.0",
  "description": "Servidor proxy SSH para acesso a VPS via WebSocket",
  "main": "server/ssh-proxy.js",
  "scripts": {
    "start": "node server/ssh-proxy.js",
    "dev": "nodemon server/ssh-proxy.js"
  },
  "dependencies": {
    "ws": "^8.16.0",
    "ssh2": "^1.15.0",
    "dotenv": "^16.4.5",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
